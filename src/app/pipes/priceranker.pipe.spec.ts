import { PricerankerPipe } from './priceranker.pipe';

describe('PricerankerPipe', () => {
  it('create an instance', () => {
    const pipe = new PricerankerPipe();
    expect(pipe).toBeTruthy();
  });
});
