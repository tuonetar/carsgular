import { DaterankerPipe } from './dateranker.pipe';

describe('DaterankerPipe', () => {
  it('create an instance', () => {
    const pipe = new DaterankerPipe();
    expect(pipe).toBeTruthy();
  });
});
